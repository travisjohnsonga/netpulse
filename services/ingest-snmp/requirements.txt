# SNMP protocol stack (pysnmp fork maintained by Lextudio)
pysnmp-lextudio>=4.4
pyasn1>=0.6

# Messaging
nats-py~=2.15

# Secrets (OpenBao / HashiCorp Vault compatible)
hvac~=2.1

# Valkey — read gNMI liveness heartbeat for adaptive polling
redis~=5.0
