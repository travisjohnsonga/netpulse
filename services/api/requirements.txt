# Web framework
Django~=6.0
psycopg[binary]~=3.1

# REST API
djangorestframework~=3.15
djangorestframework-simplejwt~=5.3
django-cors-headers~=4.3
django-filter>=24.3
drf-spectacular~=0.27

# WebSocket / async
daphne~=4.1
channels~=4.1
channels-redis~=4.3

# Task queue
celery[redis]~=5.3
django-celery-beat~=2.6

# Messaging
nats-py~=2.7

# Storage clients
influxdb-client~=1.43
# [async] pulls aiohttp so `from opensearchpy import AsyncOpenSearch` works.
opensearch-py[async]~=2.7

# Service checks (agentless synthetic monitoring).
# aiohttp is already present transitively (opensearch-py[async]); pinned here
# so the check-engine declares it directly. Stage 2 adds ICMP/DNS/SMTP probes
# (TLS/TCP/SSH-banner use the stdlib + asyncio).
aiohttp~=3.10
icmplib~=3.0     # ICMP ping (raw socket — needs NET_RAW/NET_ADMIN)
aiodns~=3.2      # async DNS resolution
aiosmtplib~=3.0  # async SMTP connect/EHLO
pyrad>=2.4       # RADIUS Access-Request health check

# Secrets
hvac~=2.1
bcrypt>=4.0  # per-collector API-key / enrollment-token hashing (already a transitive dep; pinned for hygiene)

# SSO / Single Sign-On (OAuth2). >=5.9 declares Django 6.0 support (Framework ::
# Django :: 6.0); 5.4.x predates it. Pulls social-auth-core (<5.0,>=4.8.3).
social-auth-app-django~=5.9
python-jose~=3.3       # JWT/JWS ID-token validation for SSO
PyNaCl~=1.5            # Ed25519 verification for Discord interaction webhooks (apps.core.chatops)

# Device access (config collection / interface discovery) — imported lazily
netmiko~=4.4
# paramiko is a transitive dep of netmiko; pin >=5.0.0 for CVE-2026-44405
# (Dependabot moderate). netmiko 4.7 otherwise resolves the vulnerable 4.0.0.
paramiko>=5.0.0
ntc-templates~=9.1   # TextFSM templates for show-command parsing (LLDP, interfaces)
textfsm~=2.1
ncclient~=0.6
pysnmp~=7.1
defusedxml~=0.7        # XXE/entity-expansion-safe XML parsing (apps.devices run_discovery nmap output)
requests~=2.32       # AOS-CX REST API client (apps.devices.aos_cx_client)
Jinja2~=3.1

# PDF evidence packages (apps.frameworks regulatory reports) — pure-Python, no system libs
reportlab~=4.2

# WSGI server + static files
gunicorn~=23.0   # >=23.0 fixes CVE-2024-6827 (Transfer-Encoding request smuggling)
whitenoise[brotli]~=6.7

# Testing
pytest~=9.0
pytest-django~=4.9
pyyaml~=6.0
